#include<bits/stdc++.h>
using namespace std;
int fact(int n)
{
    if(n==1)
{
    return 1 ;
}
 int ans = n*fact(n-1);
    return ans;
     

    
}
int main()
{
int ans = fact(5);
    cout<<fact(5);
}
